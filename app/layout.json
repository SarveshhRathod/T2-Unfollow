import "./globals.css";

export const metadata = {
  title: "IG Cleaner - Safe & Private",
  description: "Clean your Instagram account safely",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-slate-900">{children}</body>
    </html>
  );
}
