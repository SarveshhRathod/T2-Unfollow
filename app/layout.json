import "./globals.css";

export const metadata = {
  title: "T2 Unfollow - Safe & Private",
  description: "Secure Client-Side Instagram Manager",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-slate-950 text-white selection:bg-blue-500/30">
        {children}
      </body>
    </html>
  );
}
